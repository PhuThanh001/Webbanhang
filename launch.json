{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 9003, // Cổng mặc định của Xdebug
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}" // Thay đổi theo cấu trúc thư mục của bạn
            }
        }
    ]
}
